<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12+ SplashScreen API.
      - windowSplashScreenBackground: solid window colour.
      - windowSplashScreenAnimatedIcon: drawable centered in a 192dp icon zone
        within a 288dp canvas (see tool/gen_icons.ps1, which exports
        android12_splash.png at the correct insets per density).
      - windowSplashScreenAnimationDuration: max time the OS will hold the
        splash before forcing the app to take over (ms).
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12_splash</item>
        <item name="android:windowSplashScreenAnimationDuration">600</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
