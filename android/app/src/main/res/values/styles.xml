<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      LaunchTheme: applied while the OS is starting the process and Flutter is
      booting up. Visible only on Android 5.0 - 11. On Android 12+, the
      values-v31 variant of this style is used instead (SplashScreen API).
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!--
      NormalTheme: applied as soon as the Activity is created. Determines the
      window background while Flutter renders its first frame, and behind the
      Flutter UI thereafter.
    -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
