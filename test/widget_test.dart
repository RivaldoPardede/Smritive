import 'package:flutter_test/flutter_test.dart';

// Smoke test placeholder.
// Real feature tests will be added in Phase 4 once features are implemented.
void main() {
  test('placeholder — remove when real tests are added', () {
    expect(1 + 1, equals(2));
  });
}
